homeassistant:
  customize: !include customize.yaml
  allowlist_external_dirs:
  - /config/www/snapshots

# Enables the frontend

system_health:

default_config:

# Allows you to issue voice commands from the frontend in enabled browsers
conversation:

# Enables support for tracking state changes over time
history:

# Tracked history is kept for 10 days
recorder:
  purge_keep_days: 10

# View all events in a logbook
logbook:
  include:
    entities:
      - device_tracker.google_maps_105998041168534774687

# Track the sun
sun:

my:

energy:

scene: !include scenes.yaml

logger:
  default: info

# Text to speech
# tts: configured via UI

# Broadlink IR - Fan only (bedroom)
# Configured via UI integration as remote.broadlink_rm_pro_remote
# Fan swing/timer commands sent via script.fan_swing and script.fan_timer
# Other Broadlink devices removed during cleanup:
#   34:EA:34:8E:F8:53 (Living Room - AC/Heat/TV)
#   A0:43:B0:07:E9:E0 (Den - AC/Heat/Stereo)
#   24:DF:A7:DF:09:EE (Guestroom - AC/Heat)

template: !include template.yaml


command_line:
  - sensor:
      name: "SurePetcare Flap Stats"
      command: "sh /config/surepetcare_entries.sh"
      scan_interval: 60
      value_template: "{{ value_json.entries }}"
      json_attributes:
        - entries
        - exits
        - looked_through
        - last_entry
        - position
        - position_since
        - time_outside_today
        - time_outside_today_mins
        - outside_trips_today
        - trips_today
        - midnight_aest
      unit_of_measurement: "entries"
      icon: "mdi:cat"


sensor:
  - platform: history_stats
    name: Mew Meow Avg Time Outside
    entity_id: binary_sensor.mew_meow
    state: 'off'
    type: time
    end: '{{ now() }}'
    duration:
      days: 7

  - platform: history_stats
    name: Mew Meow Duration Inside
    entity_id: binary_sensor.mew_meow
    state: 'on'
    type: time
    start: '{{ now().replace(hour=0, minute=0, second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Mew Meow Duration Outside
    entity_id: binary_sensor.mew_meow
    state: 'off'
    type: time
    start: '{{ now().replace(hour=0, minute=0, second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Washing Machine Cycles Daily
    entity_id: sensor.electrolux_washing_machine_totalcyclecounter
    state: 'on'
    type: count
    start: '{{ now().replace(hour=0, minute=0, second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Washing Machine Cycles Weekly
    entity_id: sensor.electrolux_washing_machine_totalcyclecounter
    state: 'on'
    type: count
    start: '{{ now().replace(hour=0, minute=0, second=0) - timedelta(days=now().weekday()) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Washing Machine Cycles Monthly
    entity_id: sensor.electrolux_washing_machine_totalcyclecounter
    state: 'on'
    type: count
    start: '{{ now().replace(day=1, hour=0, minute=0, second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Washing Machine Cycles Yearly
    entity_id: sensor.electrolux_washing_machine_totalcyclecounter
    state: 'on'
    type: count
    start: '{{ now().replace(month=1, day=1, hour=0, minute=0, second=0) }}'
    end: '{{ now() }}'

#Motion statistics
  - platform: history_stats
    name: "Daily Person Count"
    entity_id: binary_sensor.front_balcony_person
    state: 'on'
    type: count
    start: '{{ now().replace(hour=0, minute=0, second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: "Daily Vehicle Count"
    entity_id: binary_sensor.front_balcony_vehicle
    state: 'on'
    type: count
    start: '{{ now().replace(hour=0, minute=0, second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: "Daily Pet Count"
    entity_id: binary_sensor.front_balcony_pet
    state: 'on'
    type: count
    start: '{{ now().replace(hour=0, minute=0, second=0) }}'
    end: '{{ now() }}'


input_number:
  spotify_volume:
    name: Volume
    icon: mdi:volume-high
    min: 0
    max: 1
    step: 0.05

input_boolean:
  guest_lamp_dim_up:
    name: "Guest Lamp Dim Up"
    initial: true
    icon: mdi:brightness-6

input_select:

  guest_lamp_scene:
    name: "Guest Lamp Scene"
    options:
      - "Read"
      - "Relax"
      - "Energise"
      - "Purple"
      - "Nightlight"
      - "Off"
    initial: "Off"
    icon: mdi:palette

  house_mode:
    name: "House Mode"
    options:
      - "None"
      - "Home"
      - "Away"
      - "Sleep"
      - "Holiday"
    initial: "None"

  bedroom_light_scenes:
    name: Bedroom Scenes
    options:
      - "Select"
      - "Energize"
      - "Read"
      - "Relax"
      - "Concentrate"
      - "Savanna sunset"
      - "Arctic aurora"
      - "Tropical twilight"
      - "Spring blossom"
    initial: "Select"
    icon: mdi:flask-outline

  upstairs_light_scenes:
    name: Upstairs Scenes
    options:
      - "Select"
      - "Energize"
      - "Read"
      - "Relax"
      - "Concentrate"
      - "Savanna sunset"
      - "Arctic aurora"
      - "Tropical twilight"
      - "Spring blossom"
      - "Crocus"
    initial: "Select"
    icon: mdi:flask-outline

  garden_light_scenes:
    name: Garden Scenes
    options:
      - "Select"
      - "Energize"
      - "Read"
      - "Relax"
      - "Concentrate"
      - "Savanna sunset"
      - "Arctic aurora"
      - "Tropical twilight"
      - "Spring blossom"
      - "Outdoor living"
    initial: "Select"
    icon: mdi:flask-outline

  den_light_scenes:
    name: Den Scenes
    options:
      - "Select"
      - "Energize"
      - "Read"
      - "Relax"
      - "Concentrate"
      - "Savanna sunset"
      - "Arctic aurora"
      - "Tropical twilight"
      - "Spring blossom"
      - "Outdoor living"
    initial: "Select"
    icon: mdi:flask-outline

  spotify_playlist:
      name: 'Playlist:'
      options:
        - Rap
        - Rock
        - Cinematic Chill-Out
        - FUTURESYNTH
        - From a Smokey Bar
        - Piano in the Background
      initial: Rap
      icon: mdi:spotify

  spotify_source:
    name: 'Source:'
    options:
      - Whole House
      - Downstairs
      - Sony
      - Living Room
      - Kitchen
      - Bedroom
    initial: Whole House
    icon: mdi:speaker-wireless

  cat_chart_range:
    name: "Cat Chart Range"
    options:
      - "7 Days"
      - "30 Days"
      - "By Month"
    initial: "7 Days"
    icon: mdi:calendar-range

input_text:

  tv_remote:
    name: "TV Remote"

  tv_tiles:
    name: "TV Channels"

  fan_remote:
    name: "Fan Remote"

  presence_detection:
    name: "Presence Detection"

media_source:

  spotify:
    name: "Spotify"

  sony_soundbar:
    name: "Sony"

  google_home:
    name: "Home"

  kitch_google_mini:
    name: "Kitchen"

  bedroom_google_mini:
    name: "Bedroom"

  whole_house:
    name: "Whole House"

  downstairs:
    name: "Downstairs"


frontend:
  extra_module_url:
    - /local/hass-hue-icons.js
  themes: !include_dir_merge_named themes


automation: !include automations.yaml
script: !include scripts.yaml
